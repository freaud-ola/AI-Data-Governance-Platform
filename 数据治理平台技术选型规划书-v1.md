# 通用数据治理平台 · 技术选型规划书

> ⚠️ **本文档为 v1 历史归档（2026-04-28），已不再维护。**  
> 最新架构请参见 [`doc/Architecture/`](./doc/Architecture/) 下的 v2 文档：  
> - 总体规划：[`doc/Architecture/00-总体规划-v2.md`](./doc/Architecture/00-总体规划-v2.md)  
> - 文档索引：[`doc/Architecture/README.md`](./doc/Architecture/README.md)  
>  
> v1 → v2 主要变更见总体规划 v2 第 1.3 节。

**版本**：v1.0  
**日期**：2026年4月28日  
**状态**：已归档（被 v2 替代）

---

## 目录

1. 项目背景与目标
2. 整体架构设计
3. 核心功能模块规划
4. 技术选型详细方案
5. 元数据接入层设计（Hive / MySQL 可配置）
6. 大模型接入层设计（可配置）
7. 部署与运维方案
8. 开发路线图
9. 风险与建议

---

## 一、项目背景与目标

### 1.1 背景

企业数仓建设日趋成熟，但元数据管理、数据质量、数据资产盘点等治理工作依然分散、手工化程度高。本项目旨在构建一套**通用、可配置的数据治理平台**，打通数仓元数据，形成数据资产知识库，并借助大模型能力提升治理自动化水平。

### 1.2 核心目标

| 目标 | 描述 |
|------|------|
| 元数据统一接入 | 支持 Hive Metastore、MySQL 等多数据源，配置化接入 |
| 数据资产知识库 | 自动生成数据字典、表血缘、业务语义注释 |
| 数据探查 | 字段级质量分析、分布统计、异常检测 |
| 数据治理工作台 | 数据质量规则管理、问题跟踪、整改闭环 |
| 大模型增强 | LLM 辅助语义理解、NL2SQL、治理建议，模型可配置 |

---

## 二、整体架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                        前端 Web 应用                         │
│         React + Ant Design Pro / Arco Design                │
└──────────────────────────┬──────────────────────────────────┘
                           │ REST / WebSocket
┌──────────────────────────▼──────────────────────────────────┐
│                      后端 API 服务层                          │
│              Python FastAPI（异步高性能）                     │
│   ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│   │ 元数据管理│ │ 数据探查 │ │ 知识库   │ │ 治理工作台   │  │
│   └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │
│   ┌────────────────────────────────────────────────────────┐ │
│   │              大模型服务层（可配置 Provider）             │ │
│   └────────────────────────────────────────────────────────┘ │
└─────────┬───────────────────────────┬───────────────────────┘
          │                           │
┌─────────▼──────────┐   ┌───────────▼────────────────────────┐
│   元数据接入层      │   │         存储层                      │
│  ┌──────────────┐  │   │  ┌──────────┐  ┌─────────────────┐ │
│  │ Hive 连接器  │  │   │  │ PostgreSQL│  │ Elasticsearch   │ │
│  │ MySQL 连接器 │  │   │  │ (元数据库)│  │ (全文检索/知识) │ │
│  │ 其他连接器   │  │   │  └──────────┘  └─────────────────┘ │
│  └──────────────┘  │   │  ┌──────────┐  ┌─────────────────┐ │
└────────────────────┘   │  │  Redis   │  │ MinIO(文件/报告) │ │
                         │  └──────────┘  └─────────────────┘ │
                         └────────────────────────────────────┘
```

---

## 三、核心功能模块规划

### 3.1 模块总览

| 模块 | 功能 | 优先级 |
|------|------|--------|
| **元数据中心** | 多数据源接入、Schema 同步、表/字段管理 | P0（必须） |
| **数据知识库** | 数据字典、业务注释、标签体系、全文搜索 | P0（必须） |
| **数据探查** | 数据分布、空值率、重复率、样本预览 | P0（必须） |
| **数据治理工作台** | 质量规则、问题单、整改流程 | P1（重要） |
| **数据血缘** | 表级/字段级血缘图、影响分析 | P1（重要） |
| **数据分类与安全** | PII 识别、安全级别标注、脱敏建议 | P1（重要） |
| **AI 助手** | NL2SQL、语义搜索、智能注释建议 | P2（增强） |
| **报告中心** | 治理报告导出、数据质量报告 | P2（增强） |

### 3.2 元数据中心

- **Schema 同步**：定时或手动触发，从数据源拉取库/表/字段/分区信息
- **变更追踪**：对比历史版本，记录 Schema 变化（新增/删除/修改字段）
- **资产目录**：树形浏览 + 列表视图，支持搜索过滤
- **数据源配置**：可视化配置连接参数，支持测试连通性

### 3.3 数据知识库

- **数据字典**：自动生成字段说明模板，支持人工填写/AI 辅助补全
- **业务术语表**：维护企业通用业务术语，与字段建立关联
- **标签体系**：自定义标签（业务域、数据域、安全级别等）
- **全文搜索**：按表名/字段名/业务含义搜索，支持模糊匹配

### 3.4 数据探查

- **基础统计**：行数、空值率、唯一值数、最大/最小值、均值
- **分布分析**：Top-N 值频次、数值分布直方图
- **异常检测**：格式异常、超出范围值、枚举违规检测
- **探查任务调度**：支持定时自动探查，结果持久化存储

### 3.5 数据治理工作台

- **质量规则引擎**：可视化配置规则（完整性/唯一性/一致性/准确性）
- **规则执行**：生成检查 SQL，对接执行引擎（SparkSQL / Hive / MySQL）
- **问题单管理**：质量问题自动创建工单，分配负责人，追踪整改
- **治理看板**：质量得分趋势、问题分类统计、整改率

### 3.6 数据血缘

- **采集方式**：SQL 解析（静态）+ 运行时日志解析（动态）
- **展示形式**：交互式有向图，支持上下游追溯
- **影响分析**：当某表变更时，自动计算下游影响范围
- **技术选型**：血缘图使用 Apache Atlas 数据模型，前端使用 AntV G6

---

## 四、技术选型详细方案

### 4.1 后端框架

| 选型 | 推荐方案 | 备选 | 理由 |
|------|----------|------|------|
| 主框架 | **Python FastAPI** | Django REST | 异步性能好，天然支持 OpenAPI，适合 AI 集成 |
| 任务调度 | **Celery + Redis** | APScheduler | 分布式任务，元数据同步/探查任务异步执行 |
| 数据库 ORM | **SQLAlchemy 2.x** | Tortoise ORM | 成熟稳定，支持多数据库 |
| 数据验证 | **Pydantic v2** | - | FastAPI 原生支持，性能优秀 |

### 4.2 前端框架

| 选型 | 推荐方案 | 备选 | 理由 |
|------|----------|------|------|
| 框架 | **React 18 + TypeScript** | Vue 3 | 生态丰富，适合复杂数据应用 |
| UI 组件 | **Ant Design Pro 5** | Arco Design | 配套 ProTable/ProForm，适合后台系统 |
| 图表 | **AntV G2 + G6** | ECharts | G2 做统计图，G6 做血缘关系图 |
| 状态管理 | **Zustand** | Redux Toolkit | 轻量，适合中型应用 |
| 构建工具 | **Vite** | Webpack | 极速热更新，开发体验好 |

### 4.3 存储层

| 用途 | 推荐方案 | 说明 |
|------|----------|------|
| 主数据库 | **PostgreSQL 15** | 存储元数据、配置、质量结果、工单 |
| 缓存 | **Redis 7** | Celery Broker + API 缓存 + Session |
| 全文检索 | **Elasticsearch 8** | 知识库全文搜索、数据字典检索 |
| 对象存储 | **MinIO** | 探查报告、导出文件存储（S3 兼容） |

> 小规模部署可省略 Elasticsearch，用 PostgreSQL 全文搜索替代。

### 4.4 血缘与图数据

| 方案 | 推荐 | 说明 |
|------|------|------|
| 血缘存储 | PostgreSQL（图关系表） | 中小规模，简单实现 |
| 血缘存储（大规模） | **Neo4j** | 数万张表以上推荐，图查询性能优 |
| SQL 解析 | **sqlglot** | Python 库，支持 Hive/MySQL/Spark SQL 解析 |
| 前端渲染 | **AntV G6** | 支持大规模图渲染，交互丰富 |

### 4.5 数据探查执行引擎

| 场景 | 方案 |
|------|------|
| Hive 数据探查 | 通过 PyHive 提交 HiveQL，结果回写 PostgreSQL |
| MySQL 数据探查 | SQLAlchemy 直连，执行统计 SQL |
| 大规模数据 | 可选接入 Spark，通过 PySpark 执行分布式探查 |

---

## 五、元数据接入层设计（可配置）

### 5.1 连接器架构

采用**插件化设计**，每种数据源实现统一接口：

```python
# 统一接口
class MetadataConnector(ABC):
    def test_connection(self) -> bool: ...
    def get_databases(self) -> List[Database]: ...
    def get_tables(self, database: str) -> List[Table]: ...
    def get_columns(self, database: str, table: str) -> List[Column]: ...
    def get_partitions(self, database: str, table: str) -> List[Partition]: ...
    def get_table_stats(self, database: str, table: str) -> TableStats: ...
```

### 5.2 Hive 连接器

```yaml
# 配置示例（config.yaml）
datasources:
  - name: "生产 Hive"
    type: hive
    enabled: true
    connection:
      host: hive-metastore.company.com
      port: 10000
      auth: KERBEROS  # 或 NONE / LDAP
      database: default
      kerberos_principal: hive/host@REALM
    sync:
      schedule: "0 2 * * *"  # 每天凌晨2点同步
      include_databases: ["ods_*", "dwd_*", "dws_*"]
      exclude_tables: ["tmp_*"]
```

**技术实现**：
- 连接库：`PyHive` + `thrift` 协议
- 元数据获取：直连 Hive Metastore Thrift 接口（更快）或 HiveQL `DESCRIBE` / `SHOW` 命令
- Kerberos 认证：通过 `requests-kerberos` / `gssapi` 支持

### 5.3 MySQL 连接器

```yaml
datasources:
  - name: "业务 MySQL"
    type: mysql
    enabled: true
    connection:
      host: mysql.company.com
      port: 3306
      user: dg_readonly
      password: "${MYSQL_PASSWORD}"  # 支持环境变量注入
      ssl: true
    sync:
      schedule: "0 3 * * *"
      include_databases: ["order_db", "user_db"]
```

**技术实现**：
- 连接库：`PyMySQL` / `mysql-connector-python`
- 元数据来源：`information_schema` 系统库（TABLES、COLUMNS、STATISTICS 表）
- 支持读取注释（`COMMENT` 字段）自动填充数据字典

### 5.4 扩展其他数据源

| 数据源 | 实现库 | 备注 |
|--------|--------|------|
| PostgreSQL | `psycopg2` | 读取 `pg_catalog` |
| Spark | `pyspark` / REST API | 通过 Spark SQL `DESCRIBE` |
| ClickHouse | `clickhouse-driver` | `system.columns` |
| HBase | `happybase` | 表结构较简单 |
| Kafka | `confluent-kafka` | Topic 级元数据 |

### 5.5 数据源配置 UI

前端提供**可视化配置页面**：
- 数据源类型下拉选择（动态加载对应表单）
- 连接参数输入 + 一键测试连接
- 同步策略配置（Cron 表达式选择器）
- 范围过滤（支持库/表名正则过滤）

---

## 六、大模型接入层设计（可配置）

### 6.1 LLM 抽象层架构

```python
# 统一 LLM 接口
class LLMProvider(ABC):
    def chat(self, messages: List[Message], **kwargs) -> str: ...
    def embed(self, text: str) -> List[float]: ...
```

### 6.2 支持的 Provider

| Provider | 模型示例 | 适用场景 |
|----------|----------|----------|
| **Anthropic Claude** | claude-3-5-sonnet | 语义理解、治理建议 |
| **OpenAI** | gpt-4o, gpt-4o-mini | NL2SQL、文档生成 |
| **Azure OpenAI** | 同 OpenAI | 企业私有化部署首选 |
| **阿里通义千问** | qwen-max | 国内合规，中文理解好 |
| **百度文心** | ERNIE-4.0 | 国内合规备选 |
| **Ollama（本地）** | llama3, qwen2.5 | 私有化、数据不出内网 |
| **自定义 API** | 任意 OpenAI 兼容接口 | 接入内部自研模型 |

### 6.3 LLM 配置示例

```yaml
llm:
  default_provider: anthropic
  
  providers:
    anthropic:
      api_key: "${ANTHROPIC_API_KEY}"
      model: claude-sonnet-4-20250514
      max_tokens: 4096
      timeout: 60
      
    openai:
      api_key: "${OPENAI_API_KEY}"
      model: gpt-4o
      base_url: https://api.openai.com/v1  # 可替换代理地址
      
    azure_openai:
      api_key: "${AZURE_API_KEY}"
      endpoint: https://your-resource.openai.azure.com
      deployment: gpt-4o
      api_version: "2024-02-15-preview"
      
    ollama:
      base_url: http://localhost:11434
      model: qwen2.5:14b
      
  # 不同功能使用不同模型（可选）
  task_routing:
    nl2sql: openai          # NL2SQL 用 OpenAI
    annotation: anthropic   # 注释生成用 Claude
    embedding: openai       # 向量嵌入
    chat: ollama            # 内部问答用本地模型
```

### 6.4 LLM 功能模块

| 功能 | 描述 | 输入 | 输出 |
|------|------|------|------|
| **智能注释生成** | 根据表名/字段名/样本值自动生成业务含义 | 元数据 | 中文注释建议 |
| **NL2SQL** | 自然语言转查询 SQL | 自然语言问题 + Schema | SQL 语句 |
| **语义搜索** | 向量化检索数据资产 | 查询文本 | 相关表/字段列表 |
| **治理建议** | 基于质量问题生成整改建议 | 质量报告 | 整改方案 |
| **数据分类** | 自动识别 PII / 敏感字段 | 字段名+样本值 | 分类标签 + 置信度 |
| **SQL 解析注释** | 解析复杂 SQL 生成血缘和说明 | SQL 代码 | 血缘关系 + 说明文档 |

### 6.5 向量知识库

- **框架**：LangChain 或 LlamaIndex（用于 RAG 问答）
- **向量数据库**：
  - 轻量：pgvector（PostgreSQL 插件）
  - 中型：Chroma（本地）
  - 生产：Milvus / Weaviate
- **用途**：数据字典语义搜索、企业知识问答

---

## 七、部署与运维方案

### 7.1 容器化部署

全服务 Docker Compose 一键启动（开发/小规模）：

```
services: frontend, backend-api, celery-worker, 
          postgresql, redis, elasticsearch, minio
```

生产环境推荐 **Kubernetes** 部署（Helm Chart 封装）。

### 7.2 配置管理

- 所有配置通过 `config.yaml` + 环境变量管理
- 敏感信息（密码/API Key）走 Kubernetes Secret 或 Vault
- 支持多环境（dev / staging / prod）配置分离

### 7.3 监控

| 层面 | 方案 |
|------|------|
| 应用监控 | Prometheus + Grafana |
| 日志 | Loki 或 ELK Stack |
| 告警 | Alertmanager + 钉钉/企微 Webhook |

---

## 八、开发路线图

### Phase 1（第 1-2 月）：基础框架 + 元数据接入

- [ ] 项目脚手架（FastAPI + React + PostgreSQL）
- [ ] 数据源配置管理 UI
- [ ] Hive 连接器开发与测试
- [ ] MySQL 连接器开发与测试
- [ ] 元数据同步任务（Celery）
- [ ] 资产目录浏览（树形 + 搜索）

### Phase 2（第 3-4 月）：知识库 + 数据探查

- [ ] 数据字典（手动编辑 + AI 辅助）
- [ ] 标签体系与数据分类
- [ ] 全文搜索（Elasticsearch 集成）
- [ ] 数据探查引擎（Hive / MySQL）
- [ ] 探查结果可视化（分布图、质量得分）

### Phase 3（第 5-6 月）：治理工作台 + 血缘

- [ ] 质量规则引擎（可视化配置）
- [ ] 质量检查任务执行与报告
- [ ] 问题单管理工作流
- [ ] SQL 解析血缘（基于 sqlglot）
- [ ] 血缘图前端展示（AntV G6）

### Phase 4（第 7-8 月）：AI 增强

- [ ] LLM Provider 抽象层 + 配置 UI
- [ ] 智能注释生成
- [ ] NL2SQL 功能
- [ ] 语义向量搜索
- [ ] 敏感字段自动识别
- [ ] 治理报告导出

---

## 九、风险与建议

| 风险 | 等级 | 建议 |
|------|------|------|
| Hive Metastore 版本差异 | 中 | 优先对接 Thrift API，兼容多版本 |
| 大表探查性能 | 高 | 探查默认采样（10万行），支持配置采样率 |
| LLM API 成本 | 中 | 结果缓存 + 批量处理，本地模型兜底 |
| 数据安全（样本数据） | 高 | 探查结果只存统计值，不存原始样本；PII 字段自动脱敏 |
| 多租户隔离 | 中 | 初期单租户，设计上预留数据隔离字段 |
| Elasticsearch 运维复杂 | 低 | 小规模可用 PostgreSQL FTS 替代，按需引入 |

---

## 附录：技术栈汇总

| 类别 | 技术 |
|------|------|
| 后端语言 | Python 3.11+ |
| Web 框架 | FastAPI |
| 任务队列 | Celery + Redis |
| 前端 | React 18 + TypeScript + Ant Design Pro |
| 图可视化 | AntV G6（血缘）+ AntV G2（图表）|
| 主数据库 | PostgreSQL 15 |
| 缓存 | Redis 7 |
| 搜索引擎 | Elasticsearch 8 |
| 对象存储 | MinIO |
| Hive 接入 | PyHive + HiveMetastoreClient |
| MySQL 接入 | PyMySQL + SQLAlchemy |
| SQL 解析 | sqlglot |
| LLM 框架 | LangChain / 自研抽象层 |
| 向量数据库 | pgvector / Chroma / Milvus |
| 容器化 | Docker Compose / Kubernetes |
| CI/CD | GitHub Actions / GitLab CI |

---

*本文档为初版评估草稿，请结合实际数据规模、团队技术栈偏好及合规要求进行调整。*